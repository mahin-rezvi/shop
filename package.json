{
  "name": "dealhaven-pro",
  "private": true,
  "workspaces": [
    "client",
    "server"
  ],
  "scripts": {
    "dev": "concurrently -n web,api -c cyan,magenta \"npm run dev --prefix client\" \"npm run dev --prefix server\"",
    "build": "npm run build --prefix client && npm run build --prefix server",
    "redis:seed": "npm run redis:seed --prefix server"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  },
  "dependencies": {
    "@sentry/nextjs": "^10.53.1",
    "firebase": "^12.13.0",
    "posthog-js": "^1.373.4"
  }
}
